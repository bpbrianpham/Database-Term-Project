CREATE TABLE `cecs323sec3og12`.`Customers` (
  `cID` INT NOT NULL,
  `eMail` VARCHAR(45) NULL,
  `phoneNumber` VARCHAR(10) NULL,
  `cDateRegistered` DATE NULL,
  `address` VARCHAR(45) NULL,
  PRIMARY KEY (`cID`)
);
